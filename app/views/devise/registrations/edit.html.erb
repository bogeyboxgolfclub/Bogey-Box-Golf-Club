<div class="w-section content-section gray">
  <div class="content-wrapper white contact">
    <div class="contact-form-box">
      <div class="contact-form-title-box">
            <div class="title medium dark">Complete your membership profile below, <%= params['first_name'] %>:</div>
          </div>
            <%= form_for(resource, as: resource_name, url: registration_path(resource_name), :html => {:name => "email-form", :class => "form", :id => "email-form"}) do |f| %>
              <%= devise_error_messages! %>
              <br>
                <%= f.hidden_field :email, autofocus: true, :value => params['email'], :class => 'w-input text-field', :style => 'text-align:center;' %>
                <%= f.hidden_field :password, autocomplete: "off", :value => "#{params['email']}_#{params['email'].size}16",  :class => 'w-input text-field', :style => 'text-align:center;' %>
                <%= f.hidden_field :password_confirmation, autocomplete: "off", :value => "#{params['email']}_#{params['email'].size}16", :class => 'w-input text-field', :style => 'text-align:center;' %>
              <script src="http://checkout.stripe.com/checkout.js" class="stripe-button"
                      data-key="<%= ENV['stripe_publishable_key'] %>"
                      data-description="Bogeybox Golf Club Membership Dues"
                      data-email="<%= params['email'] %>"
                      data-shipping-address=true
                      data-billing-address=true
                      data-amount="100"
                      data-locale="auto"
                      data-allowRememberMe=false
                      data-image="<%= image_path('bb-webclip.jpg') %>"
                      style="margin-bottom:10%;">Join The Club
              </script>
              <% end %>




          </div>
        </div>
      </div>
    </div>
  </div>

<script>
$(document).ready(function () {
   $(".stripe-button-el").trigger('click');
});
</script>

</div>
